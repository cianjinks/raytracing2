{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "Debug Viewer",
            "program": "${workspaceFolder}/build/debug/viewer",
            "preLaunchTask": "Build Viewer Debug"
        }
    ]
}